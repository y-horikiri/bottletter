<div class="row">
  <div class="col-md-9 col-md-offset-3">
    <% if logged_in? %>
    <%= form_with(model: @letter, local: true) do |f| %>
    <%= render "shared/error_messages", object: f.object %>

    <%= f.text_area :content, class: "form-control", placeholder: "どこかの誰かにメッセージを送りましょう。" %>

    <%= f.submit "流す", class: "btn btn-primary" %>
    <% end %>
    <% else %>
    <div class="card card-gray">
      <div class="card-header">
        <h2 class="card-title">かんたん登録</h2>
      </div>
      <div class="card-body">
        <%= form_with(model: @user, local: true) do |f| %>
        <%= render "shared/error_messages", object: f.object %>

        <p>名前を入力するだけですぐにBottletterを利用できます。<br>
          ブラウザを終了するとアカウントにアクセスできなくなる（※）ため、おためし用にどうぞ。<br>
          ※ブラウザの設定によって動作は異なります。</p>
        <%= f.label "名前を入力してください。" %>
        <%= f.text_field :name, class: "form-control" %>

        <%= f.submit "かんたん登録", class: "btn btn-primary" %>
        <% end %>
      </div>
    </div>
    <div class="card card-gray">
      <div class="card-header">
        <h2 class="card-title">Twitter登録</h2>
      </div>
      <div class="card-body">
        <p>Twitterで登録/ログインすると、ブラウザを終了したり、端末を変えたりしてもアカウントにアクセスできます。<br>
        </p>
        <a href="/auth/twitter" class="btn btn-primary">Twitter登録/ログイン</a>
      </div>
    </div>
    <% end %>
  </div>

  <% if logged_in? %>
  <%= render "layouts/bottle" %>
  <% end %>
</div>